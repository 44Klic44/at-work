import React from "react";
import { Layout } from "../../components/layout/Layout/Layout";

const Edit: React.FC = () => {
  return (
    <Layout>
      <h1>Редактирование</h1>
    </Layout>
  );
};

export default Edit;
